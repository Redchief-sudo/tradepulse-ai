{
  "name": "SettlementEvent",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Owner user id \u2014 every query must filter by this"
    },
    "portfolio_id": {
      "type": "string",
      "description": "Portfolio identifier for multi-portfolio tenancy"
    },
    "event_id": {
      "type": "string",
      "description": "Unique event identifier \u2014 idempotency key for settlement. For broker fills: broker_fill_id. For paper fills: generated. One broker fill maps to exactly one SettlementEvent."
    },
    "trade_intent_id": {
      "type": "string",
      "description": "The TradeIntent this event settles"
    },
    "broker_order_id": {
      "type": "string"
    },
    "broker_fill_id": {
      "type": "string",
      "description": "The broker's fill ID (for broker_paper/live mode)"
    },
    "client_order_id": {
      "type": "string"
    },
    "symbol": {
      "type": "string"
    },
    "side": {
      "type": "string",
      "enum": [
        "buy",
        "sell"
      ]
    },
    "quantity": {
      "type": "number",
      "description": "Filled quantity for this event"
    },
    "price": {
      "type": "number",
      "description": "Filled price for this event"
    },
    "notional": {
      "type": "number",
      "description": "quantity * price"
    },
    "commission": {
      "type": "number"
    },
    "fees": {
      "type": "number"
    },
    "occurred_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the fill occurred (broker timestamp or simulation time)"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "processing",
        "completed",
        "retryable_failed",
        "integrity_blocked",
        "terminal_failed"
      ],
      "default": "pending",
      "description": "Explicit lifecycle: pending, processing, retryable_failed with backoff, integrity_blocked, terminal_failed, or completed after every projection checkpoint"
    },
    "processing_owner": {
      "type": "string",
      "description": "Worker ID that claimed this event for processing"
    },
    "processing_started_at": {
      "type": "string",
      "format": "date-time",
      "description": "When processing started \u2014 used for stale lease detection"
    },
    "completed_at": {
      "type": "string",
      "format": "date-time"
    },
    "error": {
      "type": "string",
      "description": "Error message if status = failed"
    },
    "attempt_count": {
      "type": "number",
      "default": 0
    },
    "next_retry_at": {
      "type": "string",
      "format": "date-time"
    },
    "failure_kind": {
      "type": "string",
      "enum": ["transient_projection", "integrity", "retry_exhausted"]
    },
    "fill_received": {
      "type": "boolean",
      "default": true
    },
    "lot_projected": {
      "type": "boolean",
      "default": false
    },
    "cash_projected": {
      "type": "boolean",
      "default": false
    },
    "holding_projected": {
      "type": "boolean",
      "default": false
    },
    "trade_projected": {
      "type": "boolean",
      "default": false
    },
    "decision_projected": {
      "type": "boolean",
      "default": false
    },
    "intent_projected": {
      "type": "boolean",
      "default": false
    },
    "execution_mode": {
      "type": "string",
      "enum": [
        "internal_paper",
        "broker_paper",
        "paper",
        "shadow_live",
        "live"
      ]
    },
    "asset_class": {
      "type": "string"
    },
    "company_name": {
      "type": "string"
    },
    "sector": {
      "type": "string"
    },
    "strategy_id": {
      "type": "string"
    },
    "decision_id": {
      "type": "string"
    },
    "venue": {
      "type": "string"
    },
    "arrival_price": {
      "type": "number"
    },
    "decision_price": {
      "type": "number"
    },
    "submission_price": {
      "type": "number"
    },
    "first_fill_price": {
      "type": "number"
    },
    "vwap_fill_price": {
      "type": "number"
    },
    "implementation_shortfall": {
      "type": "number"
    },
    "fill_latency_ms": {
      "type": "number"
    },
    "integrity_verified": {
      "type": "boolean",
      "default": false,
      "description": "True if financial integrity verification passed after projection"
    },
    "post_settlement_projected": {
      "type": "boolean",
      "default": false,
      "description": "True after intent-owned post-settlement Holding metadata has been projected"
    },
    "realized_pnl": {
      "type": "number",
      "description": "Realized P&L computed during settlement (sells only)"
    }
  },
  "required": [
    "user_id",
    "event_id",
    "trade_intent_id",
    "symbol",
    "side",
    "quantity",
    "price",
    "status"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        }
      ]
    },
    "create": {},
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        }
      ]
    }
  }
}
